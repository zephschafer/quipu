name: 'quipu_dbt'
version: '1.0.0'
config-version: 2

profile: 'quipu_dbt'

model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

vars:
  warehouse_path: "/Users/zephschafer/Documents/GitHub/quipu-data-generator/warehouse"

models:
  quipu_dbt:
    +materialized: table
